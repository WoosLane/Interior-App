import 'package:flutter/material.dart';

class SubmitApplicationPage extends StatelessWidget {
  const SubmitApplicationPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('page'),
        toolbarHeight: 50,
      ),
    );
  }
}
